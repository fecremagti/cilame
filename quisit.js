let sources = ['source1', 'source2', 'source3'];

function mergeSources(sourcesArray) {
  // Example merging logic: concatenate all sources into a single string
  return sourcesArray.join(', ');
}
